variables:
  DOCKER_DRIVER: overlay2
  DOCKER_AUTH_CONFIG: $CI_DOCKER_AUTH_CONFIG
  NPM_REGISTRY: nexus3.tecna.pl/repository/npm-private-releases
  NPM_PROXY: nexus3.tecna.pl/repository/npm-private
  NPM_TOKEN: $TECNA_NPM_TOKEN
  UPSTREAM_REPO: $DOCKER_PROXY_URL
  DOWNSTREAM_REPO: $CI_REGISTRY
  UPSTREAM_REPO_USER: $NEXUS_USER
  UPSTREAM_REPO_PASSWORD: $NEXUS_PASSWORD
  GROUP_NAME: $CI_PROJECT_NAMESPACE
  APP_NAME: $CI_PROJECT_NAME


stages:
  - publish
  - storybook-build
  - storybook-tests
  - storybook-push
  - storybook-deploy
  - update-projects

storybook-build:
  stage: storybook-build
  image: docker.tecna.pl/node:lts
  script:
    - npm config set registry https://${NPM_PROXY}
    - npm set //${NPM_REGISTRY}:_authToken=${NPM_TOKEN}
    - npm set //${NPM_PROXY}:_authToken=${NPM_TOKEN}
    - npm ci
    - npm run build-storybook
  artifacts:
    paths:
      - storybook-static
    expire_in: 1 week
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "storybook"'


storybook-tests:
  stage: storybook-tests
  image: mcr.microsoft.com/playwright:v1.57.0-noble
  dependencies:
    - storybook-build
  variables:
    TZ: "Europe/Warsaw"
  script:
    - npm config set registry https://${NPM_PROXY}
    - npm set //${NPM_REGISTRY}:_authToken=${NPM_TOKEN}
    - npm set //${NPM_PROXY}:_authToken=${NPM_TOKEN}
    - npm ci
    - npm run vitest-storybook
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "storybook"'


storybook-push:
  stage: storybook-push
  image:
    name: gcr.io/kaniko-project/executor:v1.9.1-debug
    entrypoint: [ "" ]
  dependencies:
    - storybook-build
  before_script:
    - >
      echo "{\"auths\":{\"$CI_REGISTRY\":{\"username\":\"$CI_REGISTRY_USER\",\"password\":\"$CI_REGISTRY_PASSWORD\"}, 
      \"$UPSTREAM_REPO\":{\"username\":\"$UPSTREAM_REPO_USER\",\"password\":\"$UPSTREAM_REPO_PASSWORD\"}}}" 
      > /kaniko/.docker/config.json
  script:
    - >
      /kaniko/executor --context $CI_PROJECT_DIR 
      --dockerfile $CI_PROJECT_DIR/Dockerfile.storybook
      --build-arg NEXUS3_URL=${UPSTREAM_REPO}
      --destination ${DOWNSTREAM_REPO}/$GROUP_NAME/$APP_NAME:storybook
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "storybook"'


storybook-deploy:
  stage: storybook-deploy
  image: ${UPSTREAM_REPO}/alpine:latest
  variables:
    SSH_PRIVATE_KEY: ${SSH_PRIVATE_KEY_AUREADEV}
    ENVIRONMENT: aureadev.tecna.pl
    USER: gitlab
    ENV_REPO_PATH: /opt/dashboards/
    APP_NAME: aurea-forms-storybook
    FILE_NAME: docker-compose.yml
  rules:
    - if: '$REGISTRY_URL == "registry.tecna.pl" && ($CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "storybook")'
  before_script:
    - apk update && apk add openssh-client bash rsync
    - eval $(ssh-agent -s)
    - echo "${SSH_PRIVATE_KEY}" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan ${ENVIRONMENT} >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
  script:
    - ssh ${USER}@${ENVIRONMENT} "docker login -u ${CI_REGISTRY_USER} -p ${CI_REGISTRY_PASSWORD} ${DOWNSTREAM_REPO}; cd ${ENV_REPO_PATH}; docker compose pull ${APP_NAME}; docker compose up -d ${APP_NAME}"


aurea-forms-publish:
  image: docker.tecna.pl/node:lts
  stage: publish
  script:
    - echo "$NPM_REGISTRY_CONFIG" > ~/.npmrc
    - PACKAGE_NAME=$(node -p "require('./package.json').name")
    - PUBLISHED_VERSION=$(npm view "$PACKAGE_NAME" version --registry=https://${NPM_REGISTRY} 2>/dev/null || true)
    - NEXT_VERSION=$(node -e "const version = (process.argv[1] || require('./package.json').version).split('.').map(Number); version[2] += 1; console.log(version.join('.'))" "$PUBLISHED_VERSION")
    - npm version "$NEXT_VERSION" --no-git-tag-version --allow-same-version
    - npm install
    - npm run build
    - npm run publish:nexus
    - bash ./get-version.sh
    - source version.env
    - |
      for attempt in $(seq 1 30); do
        if npm view "vue3-schema-forms@${FORM_VERSION}" version --registry=https://${NPM_PROXY} | grep -q "^${FORM_VERSION}$"; then
          echo "vue3-schema-forms@${FORM_VERSION} is available in install registry"
          exit 0
        fi
        echo "Waiting for vue3-schema-forms@${FORM_VERSION} in install registry (${attempt}/30)"
        sleep 5
      done
      echo "vue3-schema-forms@${FORM_VERSION} is not available in install registry"
      exit 1
  artifacts:
    reports:
      dotenv: version.env
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "master"'




aurea-dashboards:
  stage: update-projects
  needs:
    - job: aurea-forms-publish
      artifacts: true
  trigger:
    project: aurea/aurea-dashboard-frontend
    branch: master
    strategy: depend
  variables:
    UPDATED_AUREA_FORMS_VERSION: "$FORM_VERSION"
    UPSTREAM_PROJECT: "$CI_PROJECT_PATH"
    UPSTREAM_COMMIT: "$CI_COMMIT_SHA"
    RUN_UPDATE_FORMS: "true"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "master"'

aurea-forms-builder:
  stage: update-projects
  needs:
    - job: aurea-forms-publish
      artifacts: true
  trigger:
    project: aurea/aurea-runtime/aurea-forms-builder
    branch: master
    strategy: depend
  variables:
    UPDATED_AUREA_FORMS_VERSION: "$FORM_VERSION"
    UPSTREAM_PROJECT: "$CI_PROJECT_PATH"
    UPSTREAM_COMMIT: "$CI_COMMIT_SHA"
    RUN_UPDATE_FORMS: "true"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "stable" || $CI_COMMIT_REF_NAME == "master"'
