import { Meta } from "@storybook/blocks";

<Meta title="Changelog" />

### 0.0.40
- fix for dictionary autoSelectOption

### 0.0.39
- internal refactoring and updates
- better way of expose function for custom controls development

### 0.0.38

- add `multiple:false` prop binding for Checkbox
- add simple version of DateTimePicker
- add new ability to use `expression` function on `text-field` component

### 0.0.37

- read only mode for date-picker
- resolve errors on query param
- add binding class for controls + tests
- add validation based on regexp expression
- expose variables for builder

### 0.0.36

- refactor DatePicker
- refactor useResolveVariable
- fix duplicated section
- add `address` field
- add hint for phone-input

### 0.0.35

- fix duplicatedSection condition
- fix func SUM in calculation
- add better condition to useConditionalRendering

### 0.0.34

- add `data-viewer` field
- fix behaviour when field is hidden
- add return options for valid form
- conditional margin in DuplicatedSection
- when returnObject search only title in Autocomplete
- add editable property in DuplicatedSection

### 0.0.33

- add ability to install custom inputs
- add to export component FormErrorVisualization and composables
- add location field
- fix tests

### 0.0.32

- Attempting to correctly upload the build to the npm repository

### 0.0.31

- Removal of erroneous yaml file uploading library to npm

### 0.0.30

- integration with v-phone-input and add phone component
- extract useLocale composable with build-in messages
- refactor tests

### 0.0.29

- add feature to use variables in label ex. label: Hello &#123;firstName:World!&#125;
- update numbers - now should be formatted properly including locale
- refactor composable useRules, useProps, useConditionalRendering
- update docs

### 0.0.28

- Add DayJs in dependency
- Add DatePicker (experimental because Vuetify component is not ready)
- Add SUM function in calculations

### 0.0.27

- Add variables resolver in props object (pinia store model)
- Add tests for variable dependency
- Improve conditional rendering (now based on pinia store model)
- Add hide fields
- Expose validation methods: scroll / messages or boolean
- Rebuild form component - add Ref with id per form, default validation
